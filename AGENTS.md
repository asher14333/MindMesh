# Local Instructions

- Use Context7 by default for third-party library, framework, SDK, CLI, and API documentation, code generation, setup, installation, migration, and configuration tasks, even when the user does not explicitly ask for documentation lookup.
- Prefer exact versioned documentation when the repo or user specifies a version.
