# Copy to .env.local and fill in values

# Canonical backend WebSocket base URL.
# Used by both the MindMesh session socket and the WebRTC signaling socket.
NEXT_PUBLIC_MINDMESH_WS_URL=ws://localhost:8000

# Optional local debug controls inside the canvas.
# NEXT_PUBLIC_MINDMESH_DEBUG=1
